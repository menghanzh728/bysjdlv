# 数字美术馆3.0 数据清洗与修复报告

**目标数据集**：历史文本数据（共计 3461 篇 Markdown 文章）  
**报告说明**：本报告记录了对全量 Markdown 文本数据进行深度清洗、排版规整及元数据结构化的全过程。

---

## 七、 2026-04-09 数据清洗与修复操作

## 2026-04-09 操作记录
1. **异常数值与排版信息清洗**
   - 目标：处理因抓取失效带来的异常互动数值（如`评论：-100`、`赞赏：-100`），并删除正文中的公众号排版信息。
   - 操作：编写Python脚本遍历 `/Users/zmh/Desktop/合集/文本数据汇总/` 下的 50 个合并文档。
   - 处理详情：
     - 将互动数据（阅读、点赞、在看、评论、转发、赞赏）中所有的负数（如 `-100`、`-1` 等）替换为 `0`，共计修复 4681 处异常数值。
     - 使用正则表达式全局删除包含以下排版特征的无用信息行：`阅读原文`、`读者留言超过24小时`、`扫码关注`、`识别二维码`、`长按二维码`、`**微信公众平台**`、`**微信视频号**`、`**新浪微博**`、`**抖音官方账号**`、`**bilibili官方账号**`，以及单独一行的 `_!_`。共计删除 4242 行公众号排版信息。
2. **简化互动数据信息**
   - 目标：将所有互动数据行（包含阅读、点赞、在看、评论、转发、赞赏）精简，仅保留“阅读量”（及可能存在的“位置”字段），去除其他冗余的互动数据指标。
   - 操作：编写Python脚本，使用正则表达式重新格式化 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档中的互动数据行。
   - 处理详情：共计修改了 3610 处互动数据行。现在数据行已由 `>阅读：xxx  点赞：xxx ... 位置：xxx` 简化为 `>阅读：xxx  位置：xxx`。
3. **去除互动数据中的位置标识**
   - 目标：将上一步简化后遗留的“位置”字段（如 `位置：10` 等代表爬虫抓取位置的无效参数）统一删除，使互动数据行仅保留纯粹的 `>阅读：xxx`。
   - 操作：编写Python脚本再次遍历并处理 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档中的互动数据行。
   - 处理详情：成功移除 3610 处“位置”参数，当前文档互动行已成为最简格式。
4. **清理残余的固定排版与版权声明**
   - 目标：删除文章末尾常出现的、不含实际内容的“官方网站：”、“官方微博：”占位行，以及大量重复的版权声明（如“文章版权归AMNUA视野所有...”）和固定文案（如“回复‘今日’将获得本日最新展览、活动讯息”等）。
   - 操作：编写Python脚本再次遍历 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档，正则匹配并剔除这些特定句式。
   - 处理详情：共计移除 2538 行无效文本，进一步提升了数据集的纯净度。

## 六、 结论与下一步建议

**清洗结论**：
历经重组、去重、去噪、时间修复与维度对齐后，当前包含 3,334 篇文章的数据集已达到高度“干净”且“结构化半成品”的标准。

**下一步建议（Next Steps）**：
1. **数据矩阵化抽取**：鉴于元数据格式（时间、标题、统一分类、正文）已被高度规范化，建议编写提取脚本，将这 50 份 Markdown 文本转化为结构化的 CSV/Excel 表格，以接入数据库或 BI 可视化工具。
2. **正文 NLP 分析**：可对纯净的正文文本开展词频分析（TF-IDF）或主题聚类（LDA），挖掘高频艺术家与历年核心议题。
3. **异常数值清理**：后续建议清洗诸如 `评论：-100` 等抓取失效带来的异常互动数值。
