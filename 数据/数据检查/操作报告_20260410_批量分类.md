# 操作报告 — merged_1.md 清洗与分类批处理

日期：2026-04-10（归档于 /Users/zmh/Desktop/合集/数据检查）

说明：本报告汇总了对合并文档、清洗流程与 `merged_1.md` 分类试运行（包含后续对 ID 23–70 的批量追加）所做的操作、验证结果、变更清单以及建议的后续步骤。该报告基于工作区内的实际文件（`/Users/zmh/Desktop/合集/精简文本数据_标准化/merged_1.md`、`/Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md`、`/Users/zmh/Desktop/合集/operation_log.md` 等）。

---

## 1. 范围与目标
- 范围：50 个合并文档（`merged_1.md` … `merged_50.md`）的清洗/标准化流程，以及 `merged_1.md` 的试分类与后续批量分类（将分类记录写入 `维度一：活动职能/merged_1_分类.md`）。
- 目标：生成一组适用于大模型与下游分析的、干净且结构化的活动文本；对 `merged_1.md` 的每条活动（约 70 条）完成模块 A/B/C 的规则化分类，并记录可追溯的操作日志（append-only）。

## 2. 已完成的关键操作（摘要）
1. 合并与分割
   - 从原始抓取的数据（共 3,461 篇文章）整理为 50 份合并文档（`merged_1.md` … `merged_50.md`），每份约 70 篇文章，总体去重后保留 3,334 篇唯一文章。
2. 全局清洗（多项）
   - 移除无意义占位符（~34,244 处 `!`）、清理失效图片链接与冗余 URL（约 1,277 处），修正异常互动数值（将负值设为 0，修复 4,681 处）、去除平台固有排版文案（共 4,242 行）等。
   - 简化互动数据，仅保留 `>阅读：xxx` 格式（处理并去除“位置”字段共 3,610 处）。
   - 时间区间标准化：统一为 `YYYY.MM.DD` 格式并补齐省略年份/月份（补全约 530 处），发现 10 处潜在“结束早于开始”逻辑异常用于后续人工复核。
3. 文本结构化
   - 按 `# File:` 定界切分为独立活动块，并在每块前添加统一分隔符 `========== [EVENT_START] ==========`；生成 `/Users/zmh/Desktop/合集/精简文本数据` 与 `/Users/zmh/Desktop/合集/精简文本数据_标准化` 两套适配文档。
4. 维度一分类（试运行与批量追加）
   - 在 `维度一：活动职能/merged_1_分类.md` 中原先已完成 ID 1–22 的试分类。
   - 根据用户指示已对 `merged_1.md` 中的剩余条目（ID 23–70）执行同一规则集的自动批量分类，并将 23–70 的 48 条记录追加到 `merged_1_分类.md`，使该文件目前包含 70 条 ID 记录（1–70）。
5. 操作日志（可追溯）
   - 所有重要步骤与数字均追加到 `/Users/zmh/Desktop/合集/operation_log.md`（append-only），并记录了“已完成 22 条试运行 + 后续追加 48 条”的时间与说明。

## 3. 变更清单（主要文件与说明）
- `/Users/zmh/Desktop/合集/精简文本数据_标准化/merged_1.md`
  - 描述：清洗并标准化后的 merged_1 源文本（保留全文，已按活动块分割）。
  - 状态：已就绪用于自动分类。

- `/Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md`
  - 描述：合并表格，包含 ID、活动名称、活动职能、表现形式、学科媒介、活动组织类型、系列标签、活动性质1/2/3、活动领域、活动主题标签等列。
  - 变更：追加了 ID 23–70 的 48 条记录（现总计 70 条）。
  - 注意：本文件当前并未明确带有 `【需人工复核】` 的文本标注（参见“核验项”章节）。

- `/Users/zmh/Desktop/合集/operation_log.md`
  - 描述：逐步记录所有重大操作（追加式）。
  - 变更：追加了关于 merged_1 的批量追加说明与复核建议说明。

- `/Users/zmh/Desktop/合集/数据检查/`（本次写入）
  - 新增：`操作报告_20260410_批量分类.md`（即本文件），用于供团队审阅与存档。

## 4. 核验结果与发现的差异
- merged_1 的条目数量：已通过文本扫描确认 `merged_1.md` 中存在约 70 个以 `# File:` 为界的活动条目；`merged_1_分类.md` 目前也包含 70 条 ID 记录（1–70），二者在数量上已对齐。
- 复核标注差异：
  - `operation_log.md` 中记录了“处理过程中约 6 条条目因信息不足被标注为需人工复核”的说明（这是操作记录里写入的意图/备注）。
  - 实际检查 `merged_1_分类.md`（当前表）时，未发现文本形式的 `需人工复核` 或 `【需人工复核】` 字段标注。因此存在一个记录/标注不同步的情况：日志里有“需复核”的说明，但分类表未显式打上该标记。
  - 建议：如果你希望在分类表中可视化这些复核条目，我可以按两种策略之一执行：
    1. 以置信度阈值（例如：当活动性质/学科媒介判定基于标题关键词不超过 1 个触发词时）标出复核条目并在表格末尾追加 `【需人工复核】`；
    2. 或者运行一次审计脚本，输出可能模糊的条目 ID 列表供人工逐一确认（推荐先审计）。

## 5. 推荐的后续动作（短列表）
- 建议 1（优先）：对 `merged_1_分类.md` 运行模糊度审计，生成“低置信度”条目清单（供人工逐条复核）——我可以马上执行并把带标注的表格更新回仓库。此步骤将消除“日志中有复核标注但表格未标注”的不一致。
- 建议 2：按批次（例如每 5 个 merged 文件为一批）把自动分类流程推广到 `merged_2.md` … `merged_50.md`，并在每批后进行抽样复核（例如每批随机抽 5 条）。这种分批推进可以把错误率控制在较低水平并便于人工把关。
- 建议 3：把 `数据清洗报告_20260408.md` 与 `数据清洗报告_20260409.md`（已存于 `/数据检查`）合并索引并在报告开头加上“关键变更摘要”以便审计人员快速定位差异。

## 6. 如需我现在做的事情（可选立即执行）
- A. 运行“模糊度审计”并把所有低置信度条目在 `merged_1_分类.md` 中标注为 `【需人工复核】`，同时把清单输出到 `/数据检查/merged_1_待复核清单.md`。
- B. 直接对 `merged_2.md` 开始同样的自动批量分类（一次处理 1 个文件或按用户指定的批量大小），并把每次批处理都追加到 `operation_log.md`。
- C. 生成一份可供人工检查的 CSV 导出（`merged_1_分类_review.csv`），包含 ID、活动名称、判定字段与判定依据（触发词），便于人工在 Excel 中批注并回写。

---

如需我现在马上执行 A/B/C 中的某项，请回复要执行的选项（例如“执行 A：先审计并标注复核条目” 或 “执行 B：开始批量分类 merged_2..merged_6”）。我会按你选择立即执行并把结果写回 `/Users/zmh/Desktop/合集/数据检查`，同时追加操作日志。