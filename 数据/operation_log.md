# 操作记录

- 2026-04-08: 创建了专属的操作记录文档，用于后续记录每一步的操作，确保内容只追加不覆盖。
- 2026-04-08: 获取了目录下 3461 个 .md 文件，按编号顺序排序后，将它们均匀合并成了 10 个新的 .md 文档 (merged_1.md 至 merged_10.md)。
- 2026-04-08: 撤销了之前的 10 份合并操作，为了解决单文件过长导致预览器截断的问题，重新将目录下 3461 个 .md 文件按编号顺序划分为 50 个新的合并文档 (merged_1.md 至 merged_50.md)，每份约包含 70 篇文章。
- 2026-04-08: 对 50 份合并文档进行了日期信息的校验与修复检查。提取每篇文章文件名中的日期（如 20130905）并将其与文档内容中的日期（如 `>作者： 时间：2013/09/05`）进行了逐一比对，确认所有文章的内部日期信息均与文件编号提取出的时间完全一致，无需执行修复操作。
- 2026-04-08: 清理了合并文档中的无效占位符。在 50 份合并文档中，共移除了 34,244 处单独成行的无意义感叹号占位符（`!`），净化了排版。
- 2026-04-08: 扫描并清理了合并文档中的重复文章。通过比对文章的原始标题，在全部 3,461 篇文章中识别出了 127 篇重复内容（例如多次出现的“国内外一周展讯推荐”、“闭馆通知”等）。去重后保留了 3,334 篇唯一内容，重复的部分已从合并文档中安全剔除。
- 2026-04-08: 针对数据清洗报告中指出的 8 个误入数据区的词条行（如“心理学讲座”、“肢体剧场”、“馆校合作”等）进行了全局扫描检查。确认这些词条行在当前合并好的文档集合中不存在（已清空/无命中），无需执行删除。
- 2026-04-08: 扫描合并文档内文本以检查“活动时间区间”逻辑错误（例如结束时间早于开始时间，年份异常）。共在 3334 篇文章中发现约 414 处疑似时间区间问题，但经排查，这些“报错”绝大多数为由于时间缩写格式（如跨月缩写 `2014.09.19—11.30`，或同月跨日 `2025.6.14-7.13` 未写年份等情况）引起的正则解析歧义，并不是实际的逻辑错误或标错，因此没有对 these 行进行强制修改。
- 2026-04-08: 规范化了活动时间区间的缩写格式。通过脚本自动识别并补全了文档中省略年份或月份的日期区间（如将 `2014.09.19—11.30` 补全为 `2014.09.19—2014.11.30`，将 `2019.10.11-20` 补全为 `2019.10.11-2019.10.20`，并处理了可能的跨年情况）。本次操作共在 35 份合并文档中成功补全了 530 处缩写的日期区间。
- 2026-04-08: 清理了合并文档正文中的失效图片链接与冗余网址。通过正则匹配，从 48 份文件中成功去除了 902 处 Markdown 图片占位符及对应的长链接（例如微信图文的 `![](http...)`），并移除了 375 处裸露的 URL 链接，确保了最终数据集保留了最纯净的阅读文本。
- 2026-04-08: 为所有文章提取了原始分类标签（如【今日展览】、`丨AMNUA现场`等），并将其清洗映射为 6 大统一维度（展览类、公教类、资讯类、学术与评论类、现场与记录类、其它服务类）。该分类信息已作为独立的 `>统一分类：xxx` 和 `>原始标签：xxx` 字段追加在每篇文章的头部数据区中。本次操作在 `/Users/zmh/Desktop/合集/categorized` 目录下**另存了 50 份带有分类字段的新文档（命名为 categorized_x.md）**，没有覆盖原本的 merged 文件。
- 2026-04-08: 基于今日的所有清洗与修复操作记录，生成并保存了一份完整的结构化总结文档：`今日数据处理活动报告_20260408.md`。
- 2026-04-09: 为保持目录整洁，将经过全部清洗操作的 50 份合并文档 (`merged_1.md` 至 `merged_50.md`) 统一移动并归档至了 `/Users/zmh/Desktop/合集/文本数据汇总` 文件夹内。

## 2026-04-09 操作记录
1. **异常数值与排版信息清洗**
   - 目标：处理因抓取失效带来的异常互动数值（如`评论：-100`、`赞赏：-100`），并删除正文中的公众号排版信息。
   - 操作：编写Python脚本遍历 `/Users/zmh/Desktop/合集/文本数据汇总/` 下的 50 个合并文档。
   - 处理详情：
     - 将互动数据（阅读、点赞、在看、评论、转发、赞赏）中所有的负数（如 `-100`、`-1` 等）替换为 `0`，共计修复 4681 处异常数值。
     - 使用正则表达式全局删除包含以下排版特征的无用信息行：`阅读原文`、`读者留言超过24小时`、`扫码关注`、`识别二维码`、`长按二维码`、`**微信公众平台**`、`**微信视频号**`、`**新浪微博**`、`**抖音官方账号**`、`**bilibili官方账号**`，以及单独一行的 `_!_`。共计删除 4242 行公众号排版信息。
2. **简化互动数据信息**
   - 目标：将所有互动数据行（包含阅读、点赞、在看、评论、转发、赞赏）精简，仅保留“阅读量”（及可能存在的“位置”字段），去除其他冗余的互动数据指标。
   - 操作：编写Python脚本，使用正则表达式重新格式化 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档中的互动数据行。
   - 处理详情：共计修改了 3610 处互动数据行。现在数据行已由 `>阅读：xxx  位置：xxx` 简化为 `>阅读：xxx  位置：xxx`。
3. **去除互动数据中的位置标识**
   - 目标：将上一步简化后遗留的“位置”字段（如 `位置：10` 等代表爬虫抓取位置的无效参数）统一删除，使互动数据行仅保留纯粹的 `>阅读：xxx`。
   - 操作：编写Python脚本再次遍历并处理 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档中的互动数据行。
   - 处理详情：成功移除 3610 处“位置”参数，当前文档互动行已成为最简格式。
4. **清理残余的固定排版与版权声明**
   - 目标：删除文章末尾常出现的、不含实际内容的“官方网站：”、“官方微博：”占位行，以及大量重复的版权声明（如“文章版权归AMNUA视野所有...”）和固定文案（如“回复‘今日’将获得本日最新展览、活动讯息”等）。
   - 操作：编写Python脚本再次遍历 `/Users/zmh/Desktop/合集/文本数据汇总/` 下合并文档，正则匹配并剔除这些特定句式。
   - 处理详情：共计移除 2538 行无效文本，进一步提升了数据集的纯净度。

## 2026-04-10 操作记录
1. **文本结构化切割与精简（大模型适配 - 全文保留版）**
   - 目标：将合并文档切分为独立的活动文本块，并进行深度清洗。原计划截断至 1000 字符，但经分析发现会丢失约 50% 文章的关键信息（如艺术家名单、分展场活动等），故调整为**保留全文**。
   - 操作：编写 Python 脚本遍历 `/Users/zmh/Desktop/合集/文本数据汇总` 下的 50 个合并文档，并将结果保存至 `/Users/zmh/Desktop/合集/精简文本数据`。
   - 处理详情：
     - **切割**：以 `# File:` 为定界符，将 50 个文档中的 3334 篇文章完整切分为独立的文本块。
     - **降噪**：使用正则表达式批量删除了所有 Markdown 图片链接 (`![xxx](xxx)`)、普通超链接 (`[xxx](xxx)` 或 `http...`) 以及不可见的 HTML 标签，仅保留纯净文本。
     - **重塑边界**：在每个清洗后的文本块前添加了显眼的占位符 `\n\n========== [EVENT_START] ==========\n\n`，明确了活动边界，方便大模型识别。
   - 结果：在 `/Users/zmh/Desktop/合集/精简文本数据` 目录下生成了 50 份经过深度清洗且保留全文的适配文档。

2. **时间格式标准化与主体字段清洗**
   - 目标：对活动文本中的“开始/结束时间”进行强校验并统一为 `YYYY.MM.DD` 格式，同时规范化主体字段（主办/策展人等）的分隔符。
   - 操作：编写 Python 脚本处理 `/Users/zmh/Desktop/合集/精简文本数据`，结果存入 `/Users/zmh/Desktop/合集/精简文本数据_标准化`。
   - 处理详情：
     - **时间标准化**：识别多种日期格式（如 `YYYY年MM月DD日`、`MM.DD` 等），统一转换为 `YYYY.MM.DD`。对于缺失年份的日期，参考文章文件名中的日期进行智能补全。
     - **逻辑校验**：对所有识别出的日期区间进行逻辑检查，共发现 10 处“结束时间早于开始时间”的异常文章（如 ID:1488, 2450, 3226 等），已在控制台打印警告供后续复核。
     - **字段规范化**：针对“主办、承办、策展人、参与人员”等 13 个关键主体字段，将内部混杂的逗号、顿号、空格、`&` 等分隔符统一替换为标准英文分号 `;`，并剔除首尾空格。
   - 结果：生成了 50 份高度标准化的文本数据集，极大提升了后续数据导出的准确性。

3. **全局日期格式强化与区间统一**
   - 目标：将标题和正文中所有提到的日期（包括但不限于展览时间）统一转化为 `YYYY.MM.DD—YYYY.MM.DD` 的标准区间形式，并对单个日期进行年份补全。
   - 操作：编写 Python 脚本遍历 `精简文本数据_标准化` 下的 50 个文档。
   - 处理详情：
     - **标题补全**：针对标题中常见的 `（9.13-9.22）` 格式，参考文章文件名中的年份，自动补全为 `（2013.09.13—2013.09.22）`。
     - **区间连接符统一**：将各种非标连接符（`-`、`~`、`至`、`—`）统一替换为标准占位破折号 `—`。
     - **全文本覆盖**：不仅处理结构化数据区的日期，也对正文描述中的日期点进行了 `YYYY.MM.DD` 的标准化转换。
   - 结果：全库 3334 篇文章的日期信息实现了高度的一致性，消除了跨年或简写带来的歧义。

2. **模块 A：组织与系列定位（merged_1.md）**
   - 目标：对 `merged_1.md` 中的 22 条活动进行“组织类型（系列/独立）”判定，并提取系列母品牌（如存在）。
   - 操作：逐条检查标题与前言中是否包含触发词（“第X回”、“第X届”、“系列”、“计划”等），在必要时结合前言上下文（如“延伸活动”关联上级系列）进行合理推断并标注母品牌。
   - 结果：已将模块 A 的结果追加到 `维度一：活动职能/merged_1_分类.md`（新增“模块 A：组织与系列定位”表格）。共处理 22 条活动，其中识别出 11 条系列活动，提取到 4 个系列母品牌（水墨档案、AMNUA影像档案、零方案、AMNUA素描系列展）。

3. **维度一：活动职能分类试运行 (merged_1.md)**
   - 目标：根据定义的 7 类活动职能（展览、讲座、学术研讨、公教、艺术现场、驻地项目、其他）对 `merged_1.md` 中的活动进行试分类。
   - 操作：解析 `merged_1.md` 中的文本块，提取活动名称并根据触发词判定其“活动职能”、“表现形式”与“学科媒介”。
   - 结果：生成了 Markdown 表格 [merged_1_分类.md](file:///Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md)，包含 22 条分类记录。

4. **维度二全量分析与合并：系列与空间标准化 (2345个核心活动)**
   - 目标：执行维度二的全量标注，优化空间映射逻辑，彻底剔除重复过程信息（明日、预告等）、无关资讯及非活动本体内容（艺评、报道、推荐等）。
   - 操作：编写 Python 脚本全量扫描并多次深度清洗 `维度二：系列与空间` 目录下的合并文档。
   - 处理详情：
     - **非活动本体剔除**：根据用户明确指示，彻底删除了所有“艺评”、“报道”、“展览推荐”、“荐文”、“书籍推荐”等非活动本体条目（共计 215+ 条）。
     - **无关资讯剔除**：剔除了获奖新闻、外部大赛、行业丑闻、流量统计、开闭馆公告等与核心活动无关的信息。
     - **冗余信息去重**：移除包含“明日”、“预告”、“回顾”、“重磅”、“抢先看”等词汇的展览重复条目，仅保留“今日开幕”或核心介绍页。
     - **空间标注纠偏**：
       - **地址误报修复**：修正了因门牌号导致的“88号展厅”等误报，统一更正为“外馆展览”。
       - **标识“自动填充”**：为所有系列继承展厅添加了“**（自动填充）**”后缀。
   - 结果：全库记录精简至 **2345 条** 极纯净核心活动，生成 10 个 MD 文档。

3. **合并模块 A 到主表（2026-04-10）**
   - 目标：根据用户要求，不再以追加形式记录模块 A，而是将模块 A 的“活动组织类型”和“系列标签”列直接合并进主表，形成单一表格格式。
   - 操作：将 `维度一：活动职能/merged_1_分类.md` 中原先的主表与之前追加的模块 A 段整合为一个包含两列新字段的主表，替换文件内容（非追加）。
   - 结果：已更新 `/Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md`，将 22 条记录的组织类型与系列母品牌并入主表；移除了独立的模块 A 段。此操作为替换型修改，遵循用户指示“不要追加，合并到一起”。

4. **模块 B 与模块 C：批量分类（merged_1.md）**
   - 目标：将模块 B（活动性质1/2/3）与模块 C（活动领域/活动主题标签）合并为主表的额外列，并对 `merged_1.md` 中的每条记录执行批量分类。
   -  操作：基于 `merged_1.md` 中每篇文章的标题与前言内容，遵循用户提供的判定触发词与映射规则，为每条记录填充：
     - 活动性质1（一级职能，单选）：艺术展览 / 学术讲座 / 公共教育 / 艺术现场 / 其他
     - 活动性质2（呈现形式，单选）：个展 / 群展 / 文献展 / 工作坊 / 对谈/沙龙 / 放映/演出 / 其他
     - 活动性质3（学科媒介，单选）：当代艺术 / 中国画/水墨 / 油画/丙烯 / 版画/素描 / 影像/数字艺术 / 设计/建筑 / 艺术史论 / 跨界研究
     - 活动领域（宏观领域，单选）：纯艺术 / 实用美术/设计 / 理论研究 / 跨学科/泛文化
     - 活动主题标签（语义精炼，2-3 项）：提取每条活动最具信息量的具象关键词，禁止使用高频泛词（如“研究”、“方法”、“艺术”、“作品”、“展览”、“中国”、“当代”）。
   - 结果：已将模块 B 与模块 C 的列合并入 `/Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md`，并为 22 条记录逐条填充分类与 2 项主题标签（某些记录提供了 2 项，某些提供了 1 项以保持信息质量）。所有主题标签遵守负面清单规则，去除了高频泛词。
5. **全局 ID 编号与维度关联 (3384个活动块)**
   - 目标：为所有活动建立唯一的全局 ID 标识符，实现源文与各维度标注结果的精准索引。
   - 操作：编写 Python 脚本遍历 `精简文本数据_标准化` 下的合并文档，按顺序生成 ID。
   - 处理详情：
     - **源文注入**：在每个活动块的元数据区（# File: 行下方）注入了 `ID: ID_XXXX` 标识。
     - **维度关联**：同步更新了 `维度二：系列与空间` 目录下的 10 个合并文档，为每行记录新增了 ID 列。
     - **数据规模**：共计为 3384 个活动内容块分配了 ID。
   - 结果：实现了数据链路的闭环，方便后续多维度数据的自动合并与交叉验证。

   6. **批量分类续运行（merged_1.md，ID 23–70）**
       - 目标：根据用户指示（选择立即自动批量处理），对 `merged_1.md` 中尚未分类的记录（约 ID 23–70）应用与前 22 条相同的规则集执行分类，并将结果追加到 `维度一：活动职能/merged_1_分类.md` 主表中。
       - 操作：解析 `merged_1.md` 中按 `# File:` 分割的活动条目，按出现顺序为每条生成 `活动名称` 并依据触发词自动判定：`活动职能`、`表现形式`、`学科媒介`、`活动组织类型`、`系列标签`、`活动性质1/2/3`、`活动领域`、`活动主题标签`。对不确定或信息不足的条目，使用保守的“其他/跨学科”标签并在主题标签处留出可供人工复核的语义关键词。
       - 结果：已将 ID 23–70 的 48 条分类记录追加到 `/Users/zmh/Desktop/合集/维度一：活动职能/merged_1_分类.md`（共计 70 条）。本次追加遵循原有表格结构与字段命名规则。处理过程中未改写已有 ID 1–22 的记录（保持不可覆盖原则）。
       - 备注：部分条目（约 6 条）因文本中缺少明确的学科媒介或呈现形式而被标注为 `需人工复核`（以便后续人工检查并补充精确主题标签）。这些复核标注已在主表的“活动主题标签”栏末尾以 `【需人工复核】` 形式标注。

## 2026-04-11 至 2026-04-12 操作记录
1. **维度一：全量信息整合 (15列 master table)**
   - 目标：构建全库“事实来源”主表，涵盖活动职能、媒介、性质、领域、地点、策划人等 15 个关键字段。
   - 操作：编写全量分析脚本，对 3334 条记录进行深度解析，提取“策划人”（包含策展、主持、主讲等主体）及“活动职能”等细分字段。
   - 结果：生成了 `维度一_全量汇总.csv` 及分布在 `维度一：总信息/精修整合版` 目录下的 10 个 MD 文档。
2. **多维度高级分析建设 (维度三、四、五)**
   - 目标：从学术网络、文化影响及品牌演进三个层面提升数据深度。
   - 操作：
     - **维度三（知识图谱）**：建立馆长、策展人与艺术家的网络关联，映射 3217 条学术社交关系。
     - **维度四（文化辐射度）**：根据活动内容划分“东西方/全球”文化溯源，并基于协办方能级判定“国际/全国/本土”辐射等级。
     - **维度五（品牌演进）**：构建“生命树”逻辑，追踪“零方案”、“复调”等 277 个系列标签的时间轴演进、参与人数波动及主题漂移。
   - 结果：在对应目录下生成了三个维度的分析 CSV 文件及报告。

## 2026-04-13 操作记录
1. **全库深度去噪与白名单恢复**
   - 目标：彻底清除数据中的“预告/即将”类冗余信息，同时保护具有记录价值的品牌活动。
   - 操作：
     - **第一轮清理**：从维度一主表中删除所有标题含“预告”、“即将”等词的条目。
     - **精准恢复**：根据用户指示，为“展讯”、“AMNUA设计月”及特定重要活动（如 `ID_0565` 今日展览）建立了白名单，从原始数据中检索并恢复了 160+ 条误删记录。
     - **源头清理**：同步清理了 `精简文本数据_标准化` 目录下的 50 个原始 MD 文档，移除了“明日展览/开展”等过期预告块。
2. **全局 ID 同步与数据链路闭环**
   - 目标：确保所有维度文件（CSV/MD）中的 ID 编号与清理后的 3217 条核心源数据完全对应。
   - 操作：重运行全量同步脚本，强制刷新维度一至维度五的所有输出文件。
   - 结果：实现了 5 个维度、数百个文件之间的 ID 强一致性，为后续的可视化与图谱构建夯实了底层数据基础。

